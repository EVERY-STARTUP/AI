"use strict";
/**
 * @swagger
 * /api/v1/periodics/:
 *   get:
 *     summary: Get all periodics
 *     tags: [FETCH DATA]
 *     parameters:
 *       - in: query
 *         name: page
 *         schema:
 *           type: integer
 *       - in: query
 *         name: size
 *         schema:
 *           type: integer
 *       - in: query
 *         name: search
 *         schema:
 *           type: string
 *       - in: query
 *         name: pagination
 *         schema:
 *           type: boolean
 *     responses:
 *       200:
 *         description: List of periodics
 */
/**
 * @swagger
 * /api/v1/destructives/:
 *   get:
 *     summary: Get all destructives
 *     tags: [FETCH DATA]
 *     parameters:
 *       - in: query
 *         name: page
 *         schema:
 *           type: integer
 *       - in: query
 *         name: size
 *         schema:
 *           type: integer
 *       - in: query
 *         name: search
 *         schema:
 *           type: string
 *       - in: query
 *         name: pagination
 *         schema:
 *           type: boolean
 *     responses:
 *       200:
 *         description: List of destructives
 */
/**
 * @swagger
 * /api/v1/productivities/:
 *   get:
 *     summary: Get all productivities
 *     tags: [FETCH DATA]
 *     parameters:
 *       - in: query
 *         name: page
 *         schema:
 *           type: integer
 *       - in: query
 *         name: size
 *         schema:
 *           type: integer
 *       - in: query
 *         name: search
 *         schema:
 *           type: string
 *       - in: query
 *         name: pagination
 *         schema:
 *           type: boolean
 *     responses:
 *       200:
 *         description: List of productivities
 */
